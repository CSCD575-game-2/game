using UnityEngine;

public class RewardProfile : MonoBehaviour
{
    public float enemyProximityWeight;
    public float allyProtectionWeight;
    public float survivalWeight;
    public float explorationWeight;
    public float resupplyWeight;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
